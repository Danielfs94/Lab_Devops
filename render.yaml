services:
  - type: web
    name: flask-api
    env: python
    plan: free
    branch: dev
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
